export class Records {
    name: string
    task_id: string
    source: string
    constructor(name: string, task_id: string, source: string) {
        this.name = name
        this.task_id = task_id
        this.source = source
    }
}